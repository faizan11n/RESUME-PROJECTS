apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notesapp-web-hpa
  namespace: notesapp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notesapp-web
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
